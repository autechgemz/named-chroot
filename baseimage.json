{
  "variables": {
    "timezone": "Asia/Tokyo",
    "image_from": "ubuntu:focal",
    "named_root": "/chroot",
    "named_user": "named",
    "named_confdir": "/etc/named",
    "named_datadir": "/var/named",
    "named_version": "9.16.23"
  },
  "builders":[
    {
      "type": "docker",
      "image": "{{user `image_from`}}",
      "run_command": ["-dit", "{{.Image}}", "/bin/bash"],
      "commit": true,
      "changes": [
        "ENV DEBIAN_FRONTEND noninteractive"
      ]
    }
  ],
  "provisioners":[
    {
      "type": "file",
      "source": "files/etc/apt/sources.list",
      "destination": "/etc/apt/sources.list"
    },
    {
      "type": "shell",
      "inline": [
        "apt-get -y update",
        "apt-get -y upgrade",
        "apt-get -y install --no-install-recommends python3 python3-apt",
	"mkdir -p /tmp/ansible-local"
      ]
    },
    {
      "type": "ansible",
      "playbook_file": "baseimage.yml",
      "user": "root",
      "ansible_env_vars": [
        "ANSIBLE_SSH_ARGS='-o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o AddKeysToAgent=no'"
      ],
      "extra_arguments": [
        "-e timezone={{user `timezone`}}",
        "-e named_root={{user `named_root`}}",
        "-e named_user={{user `named_user`}}",
        "-e named_confdir={{user `named_confdir`}}",
        "-e named_datadir={{user `named_datadir`}}",
        "-e named_version={{user `named_version`}}"
      ]
    }
  ],
  "post-processors": [
    {
      "type": "docker-tag",
      "repository": "autechgemz/named-baseimage",
      "tags": "latest"
    }
  ]
}
